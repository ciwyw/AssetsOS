# 未解决问题

> 一条一行，格式 `[模块] 问题描述`。做完的直接删掉，如果是行为变更就在 `CHANGELOG.md` 补一条。
> 模块标签：`账户` `资产` `现金` `定期` `总览` `备份` `工程` `文档`
>
> **只记实际发现的问题，不记"哪些路径还没验证"。** 验证覆盖率清单在这个项目里是无界的，写下来也没人消，只会变成噪音。唯一的例外：改了备份、持久化、删除规则或金额计算却决定跳过验证就先合，那是承担了一笔风险，值得留一行。

## 待处理

_这一节由你填。把你现在遇到的问题按上面格式写进来，或者直接口述给 agent 由它整理。_

- [ ]

## 代码核对时发现的疑点

2026-08-21 按代码逐条核对规格时发现的不一致，**尚未确认是 bug 还是可接受**：

- [ ] [总览] 主数字标签写"净资产"，实际值是全部当前资产求和，没有负债概念。当前没有负债类型所以数值正确，但命名容易误导后续开发。
- [ ] [账户] 账户金额显示 `¥0.00` 时仍可能删不掉账户——已结清定期不计入金额但算记录。阻断页有解释，但金额和可删性的口径不一致，用户第一次遇到会困惑。
- [ ] [账户] 删除确认页的影响预览是**硬编码静态值**（总资产变化恒为 `¥0.00`、恒显示"不影响"），不像资产删除那样动态计算。当前因为只有空账户能删所以结果恰好正确。
- [ ] [账户] 非现金类资产即使只有 1 项也要先点汇总行才能看到明细，多一次点击。这是刻意设计还是可以对单项做扁平化，待定。
- [ ] [账户] 阻断页里未结清定期那一行画了 `chevron.right` 但不可点，是纯装饰。
- [ ] [定期] **App 冷启动不会重排通知**，只在保存、结清、删除、恢复备份时调度。用户换机、清过通知，或首次拒绝权限后再授权，已有定期可能长期收不到提醒。
- [ ] [定期] 从现有资金创建的定期，删除时如果来源活期已经被删掉，会静默不退款，但确认页仍承诺"退回本金到原现金资产"。
- [ ] [定期] 结清时"到账资产"只读，永远落到同账户第一个活期，无法选活期+ 或其他账户。界面把它展示成一个字段，容易被误认为可选。
- [ ] [定期] 定期页展示结清时间时只显示到日期，丢掉了时分（`settledAt` 存的是完整时刻）。
- [ ] [定期] 编辑已有定期时脚注说"本金、本金来源和创建方式保存后不可修改"，但年化和起息日/到期日实际可改，文案没提。
- [ ] [定期] 顶部汇总的"预计利息"只累加进行中的定期，不含待结清，但待结清本金是单独展示的，两个口径混在一屏里。
- [ ] [现金] 证券账户下的**活期+** 不受绑定银行限制（限制只判断 `kind == .cash`），理论上可以和外部或任意账户互转。如果证券资金必须银证互转，这是漏洞。
- [ ] [现金] "从现有资金买入"的投资**不记录来源资产 ID**（定期会记），所以删除投资时不会退回来源。与定期的语义不对称。
- [ ] [备份] 恢复不是原子事务：先删全部再插入，中途失败可能留下空库或部分数据。
- [ ] [备份] 确认恢复后 sheet 立刻关闭再异步执行，用户看不到进度，失败只靠 Alert。
- [ ] [备份] 同时存在"待处理旧备份"和"上次备份失败"时，界面只显示前者，失败信息被掩盖。
- [ ] [备份] 没有"多久没备份就提醒"的判定。如果产品需要这个，当前完全没实现。
- [ ] [工程] 每次启动都有非致命的 SwiftData/CoreData 模型 checksum 警告（`Attempting to retrieve an NSManagedObjectModel version checksum while the model is still editable`），不影响运行，但做持久化改动时应确认能否消除。
- [ ] [工程] 项目没有测试 target，验证完全依赖编译 + 模拟器手工点击。

## 已知环境限制

- 默认沙箱下 `./init.sh` 可能因 `CoreSimulatorService`、`sandbox-exec` 或 Swift 宏服务假失败。这是环境问题不是代码问题，直接提权重试。
- `simctl` 不提供触摸手势能力，模拟器点击式验证要靠 `osascript` 驱动 Simulator 窗口，依赖 macOS 辅助功能授权。
