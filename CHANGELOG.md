# 变更记录

> 记录**行为变更**和**已定的决策**，让下一次会话能快速知道某个模块最近发生了什么、哪些方向已经被否决过。
>
> 约定：
> - 一条一行，格式 `[模块] 摘要`。模块标签：`账户` `资产` `现金` `定期` `总览` `备份` `工程` `文档`
> - 只写"改了什么"和"为什么"，**不写**验证日志、截图路径、命令输出——那些留在 git commit 里
> - 被用户明确否决的方向要写成 **决策**，避免以后重复踩
> - 详细的当前行为写进 `docs/spec/`，这里只留一句摘要

## 2026-09-09

- [工程] 仓库以全新历史重建并接到个人 GitHub（`git@github.com:ciwyw/AssetsOS.git`，private）。**决策：不迁移此前的本地历史。** 旧历史误提交过 `build/`，累计 3259 个文件、`.git` 达 114MB，且含 `embedded.mobileprovision`（Team ID 与真机 UDID）和 `xcuserdata` 路径；清洗成本高于其价值。新历史的初始提交 tree 与旧 HEAD 完全一致，未丢内容。
- [工程] 提交身份改为仓库级 `--local` 配置，避免继承全局的公司邮箱。真机 profile（Team ID、UDID、设备名）仍只放 `deploy.local.sh`，该文件已 ignore 且从未提交。

## 2026-08-24

- [工程] `deploy.sh` 按设备名找不到真机时，会列出当前可用设备；若只有一台则自动改用它，避免 iPhone 改名后默认部署直接失败。本地 profile 改为优先用 Identifier，不再依赖会变的设备名。设备列表不再把 `unavailable` 误判成可用。
- [工程] `deploy.sh` 设备名匹配前统一撇号（`devicectl` 输出 `’`，手写配置多是 `'`），否则 `Daraw's iPhone` 这类名字永远匹配不上。
- [工程] 修复显式传 `--device-name` 时仍被 profile 里的 Identifier 覆盖，导致写错设备名却静默装到另一台手机。**决策：命令行显式指定的设备名优先级高于 profile 存的 Identifier。**

## 2026-08-21

- [文档] 重建文档体系：`docs/spec/` 按模块拆分为功能真源（以代码为准逐条核对），本文件记变更，`TODO.md` 记未解决问题。删除 `context_1.0.md`、`feature_list.json`、`progress.md`、`session-handoff.md`。
- [文档] **决策：移除所有后续版本规划（原 V1.1 / V2.0 / V2.1 / V3.0 / V4）。** 不再按版本号组织范围，只区分"已实现"和"当前不做"。

## 2026-08-19

- [账户] 汇总行名称去掉 `X项` 计数。
- [账户] **决策：账户页主体必须是系统 `List(.insetGrouped)`，不要改成自绘标题 + 自绘白底卡片容器。** 曾按截图往自绘容器方向做视觉收口，结果白底容器让滚动观感变怪，且改之前的标题、圆角、箭头本来就是正常的。该轮改动已全部撤回。

## 2026-08-18

账户 Tab 与证券账户绑定改版（这是账户模块目前的主要形态来源）：

- [备份] SwiftData schema 升到 `AssetsOSSchemaV2`，`Account` 新增 `boundBankAccountID` / `shAAccountCode` / `szAAccountCode`；JSON 备份 `schemaVersion` 升到 2，恢复兼容旧版 1。
- [账户] 证券账户必填绑定银行账户，选填沪A / 深A（仅资料，不参与计算）；阻止删除或改类仍被证券账户绑定的银行账户。
- [账户] 账户页默认全折叠，展开状态拆成账户级和资产组级两层。**决策：点单个账户只影响当前账户**，全展开/全收起由底部"账户" Tab 重复点击负责。
- [账户] 账户行右滑动作收敛为 `添加资产 / 编辑 / 删除`，禁用 full swipe。
- [账户] 账户内资产改为两层展示：活期 / 活期+ 直接列明细，其余类型先汇总再展开；明细行去掉类型文案，投资类盈亏移到金额下方。
- [账户] 从账户内新增资产保存后，同时关闭类型选择页和编辑页，直接回账户页。
- [现金] **决策：所有新增和转账类金额输入默认空白，不预填 `0`**；未填必填金额时保存/确认置灰。编辑已有资产仍显示真实值，真实值为 `0` 时正常显示 `0`。
- [现金] 转账页重做为 `外部 / 现金资产` 两行模型，转出新增 `全部` 按钮，切回"外部"清空已选现金资产。
- [现金] **决策：证券账户活期只能和绑定银行账户下的 `活期` 互转**——不走"外部"，也不落到绑定银行的"活期+"。阻断提示在详情页点转入/转出前就弹出。
- [账户] 修复账户页右滑删除崩溃（`Invalid update: invalid number of items in section 0`）。根因是删除动作用了 `role: .destructive`，但点击后先弹阻断页/确认页而不是立即删行，SwiftUI 的乐观删行动画与列表 diff 冲突。**决策：延迟生效的 swipe action 不能用 `.destructive` role**，改普通按钮 + 显式红色 tint。
- [账户] 修复资产明细行双箭头。根因是 `assetRow` 在 `NavigationLink` label 内又手工画了一个 `chevron.right`。**决策：NavigationLink 行不要手工绘制箭头。**

## 2026-08-10

- [定期] 去掉定期 Tab 新增前的账户选择弹窗，右上角 `+` 直接打开表单，账户在表单内选。
- [定期] 移除收益预览里的"总资产变化"，只保留 `预计利息` 和 `到账本息`。
- [定期] 自动命名冲突时不再报错，直接追加纯序号：`1W / 6% / 30天 / 2`。**决策：去重只用序号，不带日期。**
- [定期] 未结清定期详情页的"本金或来源有误"改为"删除"，确认后直接删除并返回上一页，不再自动打开重建表单。资金回滚语义不变（从活期创建的退回本金，录入已有的减少总资产），都会取消未触发通知。
- [工程] 修复业务日期显示早一天。根因是业务日期按设备本地时区的 0 点保存，但展示 formatter 固定 `GMT+0`。**决策：业务日期一律按设备本地时区解释，新增日期展示必须走 `DateKit`。**
- [工程] `deploy.sh` 改为 profile 驱动的真机部署，Team / Bundle ID 通过参数覆盖，不再手改 Xcode 工程文件。个人配置放在被 gitignore 的 `deploy.local.sh`。
- [工程] `.gitignore` 忽略 `/build/`、`*.xcuserstate`、`xcuserdata/`，并把此前误入库的编译产物和 Xcode 用户态文件从索引移除。

## 2026-08-05

- [定期] 新增定期表单重做：去掉名称输入框改为自动命名（`1W / 6% / 30天`）；字段顺序改为账户 / 本金 / 年化 / 起息日 / 到期日 / 持有天数（选填）；起息日、到期日、持有天数按最近编辑的日期做锚点联动；收益预览文案改为"预计利息 / 到账本息"；备注移到表单最下。
- [定期] **决策：从现有资金创建定期时本金来源只允许活期**，选择器放在账户字段下方，选项文案为 `账户名 + 活期金额`。
- [定期] **决策：持有天数不落库**，只作为表单联动输入和展示，持久化口径仍是起息日 / 到期日。
- [定期] 修复从定期 Tab 右上角新增时缺少保存按钮（创建 sheet 缺 `NavigationStack`）。

## 2026-08-04

- 项目初始化，按当时的产品文档实现首个完整版本：三个一级页面、六种资产类型、账户与资产录入、现金转入转出、定期全生命周期与通知、JSON 备份与恢复。
